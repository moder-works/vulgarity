name: vulgarity
description: >-
  Trie-based vulgarity detection, filtering and scoring. It defeats leetspeak,
  separator and repeated-letter evasion. A matching .NET package shares the same
  seed data and the same test vectors.
version: 0.1.0
repository: https://github.com/moder-works/vulgarity

environment:
  sdk: ^3.0.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
